// Generated by `make api-generate`. Do not edit.

#pragma once

#include "tgbot/export.h"

#include <memory>
#include <nlohmann/json_fwd.hpp>
#include <string>

namespace TgBot {

/**
 * @brief This object represents a chat photo.
 */
struct ChatPhoto {
    using Ptr = std::shared_ptr<ChatPhoto>;

    /**
     * @brief File identifier of small (160x160) chat photo. This file_id can be used only for photo
     * download and only for as long as the photo is not changed.
    */
    std::string smallFileId { };

    /**
     * @brief Unique file identifier of small (160x160) chat photo, which is supposed to be the same
     * over time and for different bots. Can't be used to download or reuse the file.
    */
    std::string smallFileUniqueId { };

    /**
     * @brief File identifier of big (640x640) chat photo. This file_id can be used only for photo
     * download and only for as long as the photo is not changed.
    */
    std::string bigFileId { };

    /**
     * @brief Unique file identifier of big (640x640) chat photo, which is supposed to be the same
     * over time and for different bots. Can't be used to download or reuse the file.
    */
    std::string bigFileUniqueId { };
};

TGBOT_API void from_json(const nlohmann::json& json, ChatPhoto& value);
TGBOT_API void to_json(nlohmann::json& json, const ChatPhoto& value);

} // namespace TgBot
