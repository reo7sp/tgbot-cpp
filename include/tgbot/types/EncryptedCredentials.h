// Generated by `make api-generate`. Do not edit.

#pragma once

#include "tgbot/export.h"

#include <memory>
#include <nlohmann/json_fwd.hpp>
#include <string>

namespace TgBot {

/**
 * @brief Describes data required for decrypting and authenticating EncryptedPassportElement. See the
 * Telegram Passport Documentation for a complete description of the data decryption and
 * authentication processes.
 */
struct EncryptedCredentials {
    using Ptr = std::shared_ptr<EncryptedCredentials>;

    /**
     * @brief Base64-encoded encrypted JSON-serialized data with unique user's payload, data hashes
     * and secrets required for EncryptedPassportElement decryption and authentication
    */
    std::string data { };

    /**
     * @brief Base64-encoded data hash for data authentication
    */
    std::string hash { };

    /**
     * @brief Base64-encoded secret, encrypted with the bot's public RSA key, required for data
     * decryption
    */
    std::string secret { };
};

TGBOT_API void from_json(const nlohmann::json& json, EncryptedCredentials& value);
TGBOT_API void to_json(nlohmann::json& json, const EncryptedCredentials& value);

} // namespace TgBot
