// Generated by `make api-generate`. Do not edit.

#pragma once

#include "tgbot/export.h"

#include <cstdint>
#include <memory>
#include <nlohmann/json_fwd.hpp>

namespace TgBot {

struct User;

/**
 * @brief Describes a topic of a direct messages chat.
 */
struct DirectMessagesTopic {
    using Ptr = std::shared_ptr<DirectMessagesTopic>;

    /**
     * @brief Unique identifier of the topic. This number may have more than 32 significant bits and
     * some programming languages may have difficulty/silent defects in interpreting it. But it
     * has at most 52 significant bits, so a 64-bit integer or double-precision float type are
     * safe for storing this identifier.
    */
    std::int64_t topicId { };

    /**
     * @brief Optional. Information about the user that created the topic. Currently, it is always
     * present.
    */
    std::shared_ptr<User> user { };
};

TGBOT_API void from_json(const nlohmann::json& json, DirectMessagesTopic& value);
TGBOT_API void to_json(nlohmann::json& json, const DirectMessagesTopic& value);

} // namespace TgBot
